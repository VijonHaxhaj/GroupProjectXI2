#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Define a structure to store course information
struct Course {
    char code[20];
    char name[50];
    // Add more fields as needed (e.g., instructor, credits, etc.)
};
